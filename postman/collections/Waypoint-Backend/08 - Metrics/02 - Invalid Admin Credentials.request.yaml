$kind: http-request
url: "{{baseUrl}}/actuator/prometheus"
method: GET
description: Verifies that the protected Prometheus endpoint rejects incorrect admin Basic Auth credentials.
headers:
  Authorization: Basic ZGVmaW5pdGVseS13cm9uZzpkZWZpbml0ZWx5LXdyb25n
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status is 401', () => pm.response.to.have.status(401));
    language: text/javascript
order: 5000
