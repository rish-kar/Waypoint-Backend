$kind: http-request
url: "{{baseUrl}}/api/v1/auth/google"
method: POST
headers:
  Authorization: NotBearer invalid
  Content-Type: application/json
body:
  type: json
  content: "{}"
scripts:
  - type: afterResponse
    code: pm.test('Public route reaches request validation', () => pm.response.to.have.status(400));
    language: text/javascript
order: 3000
