$kind: collection
description: Google sign-in flow. Set googleClientId/googleClientSecret once. Run the OAuth bootstrap once to obtain googleRefreshToken; after that Google Login refreshes googleAccessToken automatically when needed.
order: 1000
