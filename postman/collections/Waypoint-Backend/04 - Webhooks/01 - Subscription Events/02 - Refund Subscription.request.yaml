$kind: http-request
description: Refunds the exact subscriptionId generated by Activate Monthly Subscription and verifies the same subscription no longer remains ACTIVE.
url: "{{baseUrl}}/api/v1/webhooks/lemonsqueezy"
method: POST
headers:
  Content-Type: application/json
  X-Signature: "{{webhookSignature}}"
body:
  type: json
  content: "{{webhookBody}}"
scripts:
  - type: beforeRequest
    code: |-
      let CryptoJS;
      try {
          CryptoJS = pm.require('npm:crypto-js@4.2.0');
      } catch (error) {
          CryptoJS = require('crypto-js');
      }
      const secret = pm.environment.get('webhookSecret');
      const subscriptionId = pm.environment.get('subscriptionId');
      const userId = pm.environment.get('userId');
      const jwt = pm.environment.get('jwt');
      if (!secret) throw new Error('Set webhookSecret in the environment.');
      if (!subscriptionId) throw new Error('Run Activate Monthly Subscription first so subscriptionId is generated for this run.');
      if (!userId) throw new Error('userId is missing. Run login or Find User by Email again.');
      if (!jwt) throw new Error('A JWT for the same user is required to verify the refund state.');

      const payload = JSON.stringify({
        meta: {
          event_name: 'subscription_payment_refunded',
          custom_data: { waypoint_user_id: userId, waypoint_plan: 'MONTHLY' }
        },
        data: {
          type: 'subscription-invoices',
          id: `subinv-${subscriptionId}-${Date.now()}`,
          attributes: {
            subscription_id: subscriptionId,
            customer_id: 'postman-customer',
            status: 'refunded',
            updated_at: new Date().toISOString()
          }
        }
      });
      pm.variables.set('webhookBody', payload);
      pm.variables.set('webhookSignature', CryptoJS.HmacSHA256(payload, secret).toString(CryptoJS.enc.Hex));
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('Webhook status is 200', () => pm.response.to.have.status(200));
      if (pm.response.code !== 200) return;

      const expectedSubscriptionId = pm.environment.get('subscriptionId');
      pm.sendRequest({
        url: `${pm.environment.get('baseUrl')}/api/v1/subscriptions/current`,
        method: 'GET',
        header: { Authorization: `Bearer ${pm.environment.get('jwt')}` }
      }, (error, response) => {
        pm.test('Refund verification request succeeded', () => {
          pm.expect(error).to.eql(null);
          pm.expect(response.code).to.eql(200);
        });
        if (error || response.code !== 200) return;
        const body = response.json();
        pm.test('Refunded subscription is not still reported ACTIVE', () => {
          if (body.externalSubscriptionId === expectedSubscriptionId) {
            pm.expect(body.status).to.eql('REFUNDED');
          } else {
            pm.expect(body.externalSubscriptionId).to.not.eql(expectedSubscriptionId);
          }
        });
      });
    language: text/javascript
order: 4000
