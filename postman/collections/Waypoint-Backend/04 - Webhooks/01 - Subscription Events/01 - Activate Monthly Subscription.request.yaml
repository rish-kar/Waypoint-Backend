$kind: http-request
description: Creates a fresh ACTIVE monthly subscription for the selected logged-in user, then verifies Waypoint persisted PREMIUM_MONTHLY for the same generated subscription ID.
url: "{{baseUrl}}/api/v1/webhooks/lemonsqueezy"
method: POST
headers:
  Content-Type: application/json
  X-Signature: "{{webhookSignature}}"
body:
  type: json
  content: "{{webhookBody}}"
scripts:
  - type: beforeRequest
    code: |-
      let CryptoJS;
      try {
          CryptoJS = pm.require('npm:crypto-js@4.2.0');
      } catch (error) {
          CryptoJS = require('crypto-js');
      }
      const secret = pm.environment.get('webhookSecret');
      const userId = pm.environment.get('userId');
      const jwt = pm.environment.get('jwt');
      const variantId = pm.environment.get('monthlyVariantId');
      if (!secret) throw new Error('Set webhookSecret in the environment.');
      if (!userId) throw new Error('Run login or Find User by Email first so userId is populated.');
      if (!jwt) throw new Error('A JWT for the same user is required to verify the resulting subscription state.');
      if (!variantId) throw new Error('Set monthlyVariantId in the environment.');

      const now = new Date();
      const renewsAt = new Date(now.getTime() + 30 * 24 * 60 * 60 * 1000);
      const safeUser = String(userId).replace(/[^a-zA-Z0-9]/g, '').slice(0, 12);
      const subscriptionId = `postman-${safeUser}-${Date.now()}`;
      pm.environment.set('subscriptionId', subscriptionId);

      const payload = JSON.stringify({
        meta: {
          event_name: 'subscription_created',
          custom_data: { waypoint_user_id: userId, waypoint_plan: 'MONTHLY' }
        },
        data: {
          type: 'subscriptions',
          id: subscriptionId,
          attributes: {
            customer_id: `postman-customer-${safeUser}`,
            product_id: 'postman-product',
            variant_id: variantId,
            status: 'active',
            updated_at: now.toISOString(),
            renews_at: renewsAt.toISOString(),
            ends_at: null
          }
        }
      });
      pm.variables.set('webhookBody', payload);
      pm.variables.set('webhookSignature', CryptoJS.HmacSHA256(payload, secret).toString(CryptoJS.enc.Hex));
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('Webhook status is 200', () => pm.response.to.have.status(200));
      if (pm.response.code !== 200) return;

      const expectedSubscriptionId = pm.environment.get('subscriptionId');
      const expectedUserId = pm.environment.get('userId');
      pm.sendRequest({
        url: `${pm.environment.get('baseUrl')}/api/v1/subscriptions/current`,
        method: 'GET',
        header: { Authorization: `Bearer ${pm.environment.get('jwt')}` }
      }, (error, response) => {
        pm.test('Subscription verification request succeeded', () => {
          pm.expect(error).to.eql(null);
          pm.expect(response.code).to.eql(200);
        });
        if (error || response.code !== 200) return;
        const body = response.json();
        pm.test('Monthly subscription persisted for the intended user', () => {
          pm.expect(body.plan).to.eql('PREMIUM_MONTHLY');
          pm.expect(body.status).to.eql('ACTIVE');
          pm.expect(body.premium).to.eql(true);
          pm.expect(body.externalSubscriptionId).to.eql(expectedSubscriptionId);
        });
        pm.test('Verification still targets the selected user', () => pm.expect(pm.environment.get('userId')).to.eql(expectedUserId));
      });
    language: text/javascript
order: 2000
