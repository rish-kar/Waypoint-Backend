{
  "info": {
    "_postman_id": "c8116d78-8d31-49a5-a2d7-6420f97d7730",
    "name": "Waypoint Backend - Friends & Family AI",
    "description": "Focused checks for Premium Special / Friends & Family AI. Personal requests expose only the signed-in user's abstract quota in rupees. The admin request exposes the complete pool and every Special grant user's account, grant and usage details in rupees.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "01 - Current Subscription",
      "event": [
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "pm.test('Status is 200', () => pm.response.to.have.status(200));",
              "const body = pm.response.json();",
              "pm.test('Subscription payload is present', () => pm.expect(body).to.be.an('object'));",
              "pm.environment.set('familyAiCurrentPlan', String(body.plan || ''));",
              "pm.environment.set('familyAiSubscriptionStatus', String(body.status || ''));"
            ]
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [
          { "key": "Authorization", "value": "Bearer {{jwt}}" },
          { "key": "X-Request-ID", "value": "postman-family-ai-subscription" }
        ],
        "url": "{{baseUrl}}/api/v1/subscriptions/current",
        "description": "Confirms which subscription/plan the authenticated account currently receives. Premium Special should be returned for an active Friends & Family grant."
      }
    },
    {
      "name": "02 - Personal Friends & Family AI Usage",
      "event": [
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "pm.test('Status is 200', () => pm.response.to.have.status(200));",
              "const body = pm.response.json();",
              "pm.test('Personal quota shape is valid', () => {",
              "    pm.expect(body.specialAccess).to.be.a('boolean');",
              "    pm.expect(body.requestTokenLimit).to.be.a('number');",
              "    pm.expect(body.monthlyAllowanceRupees).to.be.a('number');",
              "    pm.expect(body.spentRupees).to.be.a('number');",
              "    pm.expect(body.remainingRupees).to.be.a('number');",
              "    pm.expect(body.usagePercent).to.be.a('number');",
              "    pm.expect(body.periodKey).to.match(/^\\d{4}-\\d{2}$/);",
              "    pm.expect(body.resetsAt).to.be.a('string').and.not.empty;",
              "    pm.expect(['ACTIVE', 'LIMIT_REACHED', 'NOT_SPECIAL']).to.include(body.status);",
              "});",
              "pm.test('Internal accounting units are not exposed', () => {",
              "    pm.expect(body).to.not.have.property('monthlyAllowanceMicrorupees');",
              "    pm.expect(body).to.not.have.property('spentMicrorupees');",
              "    pm.expect(body).to.not.have.property('remainingMicrorupees');",
              "});",
              "pm.test('Personal response does not expose pool or other users', () => {",
              "    pm.expect(body).to.not.have.property('monthlyPoolRupees');",
              "    pm.expect(body).to.not.have.property('activeSpecialUsers');",
              "    pm.expect(body).to.not.have.property('users');",
              "});",
              "if (body.specialAccess) {",
              "    pm.expect(body.requestTokenLimit).to.eql(5000);",
              "    pm.expect(body.monthlyAllowanceRupees).to.be.above(0);",
              "}",
              "pm.environment.set('familyAiSpecialAccess', String(body.specialAccess));",
              "pm.environment.set('familyAiRequestTokenLimit', String(body.requestTokenLimit));",
              "pm.environment.set('familyAiAllowanceRupees', String(body.monthlyAllowanceRupees));",
              "pm.environment.set('familyAiSpentRupees', String(body.spentRupees));",
              "pm.environment.set('familyAiRemainingRupees', String(body.remainingRupees));",
              "pm.environment.set('familyAiUsagePercent', String(body.usagePercent));",
              "pm.environment.set('familyAiResetsAt', String(body.resetsAt));",
              "pm.environment.set('familyAiStatus', String(body.status));"
            ]
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [
          { "key": "Authorization", "value": "Bearer {{jwt}}" },
          { "key": "Accept", "value": "application/json" },
          { "key": "X-Request-ID", "value": "postman-family-ai-usage" }
        ],
        "url": "{{baseUrl}}/api/v1/ai/family-usage",
        "description": "Returns only the authenticated user's abstract Friends & Family quota in rupees. It intentionally hides internal accounting units, the configured shared pool, active Special-user count and all other users."
      }
    },
    {
      "name": "03 - General AI Usage",
      "event": [
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "pm.test('Status is 200', () => pm.response.to.have.status(200));",
              "const body = pm.response.json();",
              "pm.test('General AI entitlement is exposed', () => pm.expect(body.cloudAiAllowed).to.be.a('boolean'));"
            ]
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [
          { "key": "Authorization", "value": "Bearer {{jwt}}" },
          { "key": "X-Request-ID", "value": "postman-general-ai-usage" }
        ],
        "url": "{{baseUrl}}/api/v1/ai/usage",
        "description": "Shows the normal Cloud AI entitlement/trial usage alongside the separate Friends & Family quota endpoint."
      }
    },
    {
      "name": "04 - Admin Full Family AI Pool",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "type": "text/javascript",
            "exec": [
              "let CryptoJS;",
              "try { CryptoJS = pm.require('npm:crypto-js@4.2.0'); } catch (error) { CryptoJS = require('crypto-js'); }",
              "const adminId = pm.environment.get('adminId');",
              "const adminPassword = pm.environment.get('adminPassword');",
              "if (!adminId || !adminPassword) throw new Error('Set adminId and adminPassword in the Waypoint Local environment.');",
              "pm.environment.set('adminBasicAuth', CryptoJS.enc.Base64.stringify(CryptoJS.enc.Utf8.parse(`${adminId}:${adminPassword}`)));"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "pm.test('Status is 200', () => pm.response.to.have.status(200));",
              "const body = pm.response.json();",
              "pm.test('Admin receives the complete Family AI pool', () => {",
              "    pm.expect(body.monthlyPoolRupees).to.be.a('number').and.above(0);",
              "    pm.expect(body.poolSpentRupees).to.be.a('number').and.at.least(0);",
              "    pm.expect(body.poolRemainingRupees).to.be.a('number').and.at.least(0);",
              "    pm.expect(body.poolUsagePercent).to.be.a('number').and.within(0, 100);",
              "    pm.expect(body.activeSpecialUsers).to.be.a('number').and.at.least(0);",
              "    pm.expect(body.requestTokenLimit).to.eql(5000);",
              "    pm.expect(body.periodKey).to.match(/^\\d{4}-\\d{2}$/);",
              "    pm.expect(body.resetsAt).to.be.a('string').and.not.empty;",
              "    pm.expect(body.users).to.be.an('array');",
              "});",
              "pm.test('Internal accounting units are not exposed', () => {",
              "    pm.expect(body).to.not.have.property('monthlyPoolMicrorupees');",
              "    pm.expect(body).to.not.have.property('poolSpentMicrorupees');",
              "    pm.expect(body).to.not.have.property('poolRemainingMicrorupees');",
              "});",
              "pm.test('Admin receives complete per-user and grant details', () => {",
              "    body.users.forEach(user => {",
              "        ['grantId','userId','email','displayName','pictureUrl','phoneNumber','phoneCountryCode','provider','providerUserId','persistedPlan','userCreatedAt','userUpdatedAt','lastLoginAt','active','validUntil','reason','grantedBy','grantedAt','revokedBy','revokedAt','monthlyAllowanceRupees','spentRupees','remainingRupees','usagePercent','status'].forEach(key => pm.expect(user).to.have.property(key));",
              "        pm.expect(user).to.not.have.property('monthlyAllowanceMicrorupees');",
              "        pm.expect(user).to.not.have.property('spentMicrorupees');",
              "        pm.expect(user).to.not.have.property('remainingMicrorupees');",
              "        pm.expect(['ACTIVE', 'LIMIT_REACHED', 'EXPIRED', 'REVOKED']).to.include(user.status);",
              "        pm.expect(user.spentRupees).to.be.a('number').and.at.least(0);",
              "        pm.expect(user.remainingRupees).to.be.a('number').and.at.least(0);",
              "        pm.expect(user.usagePercent).to.be.a('number').and.within(0, 100);",
              "    });",
              "});",
              "pm.environment.set('familyAiAdminPoolRupees', String(body.monthlyPoolRupees));",
              "pm.environment.set('familyAiAdminPoolSpentRupees', String(body.poolSpentRupees));",
              "pm.environment.set('familyAiAdminPoolRemainingRupees', String(body.poolRemainingRupees));",
              "pm.environment.set('familyAiAdminActiveUsers', String(body.activeSpecialUsers));",
              "pm.environment.set('familyAiAdminUserCount', String(body.users.length));"
            ]
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [
          { "key": "Authorization", "value": "Basic {{adminBasicAuth}}" },
          { "key": "Accept", "value": "application/json" },
          { "key": "X-Request-ID", "value": "postman-admin-family-ai-usage" }
        ],
        "url": "{{baseUrl}}/api/v1/admin/family-ai",
        "description": "Admin-only complete pool view in rupees: configured pool, total usage, remaining budget, active user count and every Special grant user's account, grant and usage details. Set adminTotp in production; this collection automatically sends it as X-Admin-TOTP on /api/v1/admin/** requests."
      }
    }
  ],
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "if (!pm.environment.get('baseUrl')) throw new Error('Select the Waypoint Local environment.');",
          "const requestUrl = pm.request.url.toString();",
          "const adminTotp = String(pm.environment.get('adminTotp') || '').trim();",
          "if (adminTotp && requestUrl.includes('/api/v1/admin/')) {",
          "    pm.request.headers.upsert({ key: 'X-Admin-TOTP', value: adminTotp });",
          "} else {",
          "    pm.request.headers.remove('X-Admin-TOTP');",
          "}"
        ]
      }
    }
  ]
}
